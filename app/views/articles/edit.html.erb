<h1>Edit article</h1>

<%= render 'form' %>

<div class="container">
  <td><%= link_to 'Delete', article_path(@article), method: :delete, data: {confirm: 'Are you sure?'}, :class => "btn btn-default" %></td>
</div>

<%= link_to 'Back', articles_path, :class => "btn btn-default" %>